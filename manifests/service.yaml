apiVersion: v1
kind: Service
metadata:
  name: slackbot-worker
  labels:
    app: slackbot
    tier: worker
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http-server
  - port: 6060
    targetPort: debug-port
  selector:
    app: slackbot
    tier: worker
