apiVersion: v1
kind: Service
metadata:
  name: slackbot-worker
  labels:
    app: slackbot
    tier: worker
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http-server
  selector:
    app: slackbot
    tier: worker
